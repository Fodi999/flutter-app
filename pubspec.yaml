name: sushi_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  http: ^0.13.6
  provider: ^6.1.1
  uuid: ^4.0.0       # ✅ UUID для ID
  logger: ^2.0.2     # ✅ ДЛЯ ЦВЕТНЫХ И СТРУКТУРИРОВАННЫХ ЛОГОВ

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true



